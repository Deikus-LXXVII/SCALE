interface:
  display_name: "S.C.A.L.E. Orchestrator"
  short_description: "Route SCALE agents with clear ownership"
  default_prompt: "Use $scale-orchestrator. For compound work, delegate implementation to a bounded SCALE executor first; keep the main session focused on routing and one batched acceptance check."
policy:
  allow_implicit_invocation: true
