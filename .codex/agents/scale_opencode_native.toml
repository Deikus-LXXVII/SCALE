name = "scale_opencode_native"
description = "Safe native fallback profile; experimental global OpenCode gateway is disabled."
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
sandbox_mode = "workspace-write"
developer_instructions = """
The experimental global OpenCode gateway is disabled because user-level
openai_base_url routes every Codex model through one process. Execute the
bounded task on native Luna and keep context minimal. Do not claim OpenCode Go
or DeepSeek execution. External OpenCode dispatch requires an explicit
privacy-gated work order; critical and sensitive decisions stay on Sol/Terra.
"""
